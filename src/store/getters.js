
export default {
    /**
     * 导航
     * @param state
     * @returns {default.newMenuListContent|(function(*))|Array|*}
     */
    newMenuListContent(state) {
        return state.newMenuListContent
    }
}